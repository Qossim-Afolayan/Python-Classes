# A list is a data structure that stores an ordered sequence of objects (array)
#An object is a data structure (tools for organizing data)

#Class: a blueprint that an object is made from

empty = [] #literal that creates a string
empty = list() #to convert

#element or item: objects in a list

sodas = ["Coke", "Pepsi", "Mirinder"]

print(len(sodas))

quarterly_revenues = [15000, 10000, 12000, 9000]
print(len(quarterly_revenues))

stock_prices = [345.65, 234.4]
print(len(stock_prices))

user_settings = [True, False, True, False, True]
print(len(user_settings))
